package com.vfergus.common.recyclerView.decoration.spacing.span;

import android.support.annotation.NonNull;
import android.view.View;

public interface SpanIndexLookup {
    int getSpanIndex(@NonNull View view);
}