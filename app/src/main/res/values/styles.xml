<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Title">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Title</item>
    </style>

    <style name="AppTheme.Title.Dark" parent="AppTheme.Title">
        <item name="android:textColor">@color/color_black</item>
    </style>

    <style name="AppTheme.Title.Light" parent="AppTheme.Title">
        <item name="android:textColor">@color/color_white</item>
    </style>

    <style name="AppTheme.Button.LoginFacebook">
        <item name="android:text">@string/login_with_fb</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:drawableStart">@drawable/com_facebook_button_icon_white</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:background">@drawable/ic_button_login</item>
        <item name="android:textColor">@color/color_white</item>
        <item name="android:clickable">true</item>
        <item name="android:foreground">?android:selectableItemBackgroundBorderless</item>
    </style>

    <style name="AppTheme.Button"/>

</resources>
